# Essential packages compatible with Python 3.13
# Note: Install portaudio first: brew install portaudio
# Alternative: use sounddevice instead of PyAudio
faster-whisper
transformers
torch
torchaudio
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.11.0
librosa
soundfile
# PyAudio  # Comment out if having issues, use sounddevice instead
sounddevice
requests